# Environment settings
# This determines which environment the app runs in
# Options: development, test, production
NODE_ENV=development

# Application Settings
# Version number displayed in the UI
VITE_APP_VERSION=1.0.0

# Local storage key for saving flowchart data
VITE_LOCAL_STORAGE_KEY=flowchart_g_data

# Optional: Public URL for deployment (uncomment if needed)
# VITE_PUBLIC_URL=http://localhost:5173 

# Deployment Notes:
# When deploying to Vercel, you should set the VITE_LOCAL_STORAGE_KEY
# in the Environment Variables section of your Vercel project settings.
# No other environment variables are required for deployment. 